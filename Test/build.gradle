plugins {
    id 'java'
}

group = 'com.whr.spring'
version = '7.0.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
    implementation project(':spring-context')
}

test {
    useJUnitPlatform()
}